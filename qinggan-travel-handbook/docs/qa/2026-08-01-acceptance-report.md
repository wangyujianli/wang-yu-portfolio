# 《向西而行》完整交付验收记录

验收日期：2026-08-01  
验证环境：Windows、Node.js 24.15.0、npm 11.12.1、Vite 8.2.0

## 工程关

- Vue 3、Vite、TypeScript、Vue Router、Pinia、GSAP、Lucide Vue 正常工作。
- 内容、路由、服务、状态与组件分层清晰。
- `npm install` 完成，生产依赖审计为 0 个已知漏洞。
- ESLint、Vue TypeScript 检查与生产构建均通过。

## 功能关

- 39 个公开页面与地点详情路由在浏览器逐一打开，无 404、空标题或页面级横向溢出。
- 高德真实地图成功加载；无 Key、加载失败与缓存/重试由自动测试覆盖，页面可切换完整图文路线。
- 地图弹层、四条参考路线、双维度筛选、九天参考、周边可玩、沿途彩蛋与准备清单可用。
- 真实地图默认只按青甘路线取景，杭州航线不参与自动缩放；所有路线点显示地点名称，按钮可直接点按和键盘触发。
- 已去过状态刷新后保留；足迹测试后已恢复原始状态。
- 天气成功、六小时缓存、失败后旧缓存与无缓存状态均由测试覆盖。
- 30个地点均已接入统一的“开放 · 门票 · 预约 · 联系”模块；电话可直拨，微信渠道可复制，正式外链在新窗口打开。
- 出发前确认中心新增四组门票预约清单和正式景区开放时间速查；筛选、详情跳转与“不是实时营业状态查询”说明已验证。

## 内容关

- 30 个地点具有结构化游览价值、玩法、天气替代、摄影、预约、现场信息、住宿映射与取舍建议。
- 37 张获准使用的本地原图生成 111 个 WebP；塔尔寺单图经使用者明确批准后单独放行，德令哈从补充参考包按文件名白名单合并；另 1 张社交平台来源文件与 1 张不适合公开展示的动物画面未发布。
- WebP 均已检查尺寸，未携带 EXIF/XMP 元数据。
- 扁都口在暂未匹配到批量导入实景图时，会自动使用已有本地主图 `/images/places/biandukou.jpg`，详情画廊不再出现空白。
- 住宿数据通过住宿节点复用；页面不显示价格、促销、评分或站内支付。
- 页面未出现任何内部用户画像标签。
- 莫高窟与嘉峪关按淡旺季拆分开园、停售/停止检票、最晚入园和闭园；张掖与茶卡已采用覆盖8月3日至9日的2026年官方运营公告。
- 乌素特水上雅丹、大柴旦翡翠湖、察尔汗盐湖采用近期官方恢复开放信息；官方未公开8月分项时刻的字段保持为空。
- 青海湖区总体无统一时刻；二郎剑按月发布，网页端未检索到可验证的8月表，因此页面直接提供“青海湖景区”公众号、“智游青海湖”小程序和咨询电话，不沿用6月数字。
- 公共道路、城市与生态准入地点没有使用“全天开放”；缺少可靠时刻时保留“临行确认”。

## 视觉关

- 1440 桌面、1024 展开屏、390 与 360 普通手机均完成浏览器截图检查。
- 拍照宝典与用户参考图在 1680 × 943 同视口并排比较，结论记录在项目根目录 `design-qa.md`。
- 地图地点名称在展开屏直接可读，边缘节点预留取景空间；普通手机保持完整地图与图文路线切换。
- 单图、轮播、多图缩略图、拍照攻略完整图灯箱、住宿横滑与抽屉均完成真实交互检查。
- 新票务模块已在1024展开屏与390普通手机完成折叠/展开、两栏/单栏和页面横向溢出检查；截图为 `34` 至 `38` 号验收图。
- 茶卡、张掖与青海湖详情页完成浏览器实页复核；茶卡首屏显示07:20—21:30、20:00停止检票和“非强制预约”，展开后能打开本次采用的公告来源。
- 浏览器控制台最终 error 0、warning 0；1280宽度检查未出现页面横向溢出。

## 发布关

- `npm run lint`：通过。
- `npm run type-check`：通过。
- `npm run test:run`：29 个测试文件、96 项测试全部通过。
- `npm run images:verify`：111 个 WebP 全部通过。
- `npm run build`：通过，产物位于 `dist/`。
- README 已说明启动、高德 Key、天气、图片导入、住宿映射、内容修改、localStorage 与静态部署。

验收结果：通过。
