name: Deploy portfolio and Qinggan handbook

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: qinggan-travel-handbook/package-lock.json

      - name: Install dependencies
        working-directory: qinggan-travel-handbook
        run: npm ci

      - name: Verify and build handbook
        working-directory: qinggan-travel-handbook
        env:
          VITE_BASE_PATH: /wang-yu-portfolio/qinggan-travel-handbook/
        run: |
          npm run lint
          npm run test:run
          npm run images:verify
          npm run build

      - name: Assemble Pages site
        run: |
          mkdir -p _site/qinggan-travel-handbook
          cp index.html styles.css script.js .nojekyll _site/
          cp -R assets output _site/
          cp -R qinggan-travel-handbook/dist/. _site/qinggan-travel-handbook/
          touch _site/.nojekyll

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./_site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
